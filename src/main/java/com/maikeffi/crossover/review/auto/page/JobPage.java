package com.maikeffi.crossover.review.auto.page;

import org.openqa.selenium.WebDriver;

public class JobPage extends AbstractPageObject {
    public JobPage(WebDriver webDriver, String pageUrlKey) {
        super(webDriver, pageUrlKey);
    }
}
